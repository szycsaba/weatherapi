# weatherapi
Laravel project that fetches current weather data from the OpenWeather API every 10 minutes based on city latitude and longitude, and stores the results in a database. Provides REST API endpoints to list stored weather records and to search by city name, returning data from the last 24 hours.
